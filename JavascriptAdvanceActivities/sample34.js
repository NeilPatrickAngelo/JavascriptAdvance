const person = {
  firstName: "John",
  lastName: "Doe",
};

// Add a new property
person.age = 30;
person.height = 170;
person.weight = 73;

console.log(person.age); // Outputs: 30
console.log(person.height);
console.log(person.weight);
console.log(person);