const text = "Hello, world!";
const pattern = /world/;
const replacement = "universe";
const updatedText = text.replace(pattern, replacement);
console.log(updatedText); // Output: "Hello, universe!"