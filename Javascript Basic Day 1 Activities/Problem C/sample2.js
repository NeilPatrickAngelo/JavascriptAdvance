// Demonstrating String Length
const myString = "Hello, World!";
const stringLength = myString.length;
console.log(`The length of the string "${myString}" is ${stringLength} characters.`);

 

// Demonstrating Escape Characters
const escapedString = "This is a \"quoted\" string.";
console.log(escapedString);

 

// Breaking Long Code Lines
const longCodeLine = "This is a long code line that needs to be broken " +
    "into multiple lines for better readability and maintainability. " +
    "You can use the plus (+) sign to concatenate strings across lines.";
console.log(longCodeLine);